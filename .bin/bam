#!/usr/bin/env node

var program = require('commander');

program
   .version('0.0.1')
   .command('install', 'Install Bamburgs').alias('i')
   .command('update', 'Install Bamburgs').alias('u')
   .parse(process.argv);
